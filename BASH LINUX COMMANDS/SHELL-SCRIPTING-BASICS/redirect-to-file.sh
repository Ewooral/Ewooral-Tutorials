#! /bin/bash
echo "I am being redirected to the new-script file"  > ./new-script.txt
# only one greater than sign overwrites the file's contents


# save the input of a user into a file
cat > new-file.txt

## append to a file

cat >> new-file.txt